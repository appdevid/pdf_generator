version: "3.9"
services:
  html2pdf-api:
    container_name: html2pdf-api
    ports:
      - "3020:3020"
    volumes:
      - ./tmp:/tmp
    restart: unless-stopped
    stdin_open: true
    tty: true
    build:
      context: ./app
      dockerfile: Dockerfile
    command: node server.js
